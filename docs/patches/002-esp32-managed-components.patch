From: pyDirect <pyDirect@scripto.studio>
Subject: [PATCH 2/2] esp32: Add managed components for pyDirect modules

Add ESP-IDF managed components required by pyDirect:
- joltwallet/littlefs: for modwebfiles.c (esp_vfs_littlefs)
- husarnet/esp_husarnet: for Husarnet VPN module
- espressif/cbor: for modwebrepl.c (CBOR encoding)
- espressif/esp_peer: for modwebrtc.c (WebRTC peer connection)
- espressif/iot_usbh_modem: for usbmodem module (USB 4G modem)

---
 ports/esp32/main/idf_component.yml | 12 ++++++++++++
 1 file changed, 12 insertions(+)

diff --git a/ports/esp32/main/idf_component.yml b/ports/esp32/main/idf_component.yml
index cb14ffde6..7d89d955e 100644
--- a/ports/esp32/main/idf_component.yml
+++ b/ports/esp32/main/idf_component.yml
@@ -10,5 +10,17 @@ dependencies:
     rules:
       - if: "target == esp32"
       - if: "idf_version >=5.3"
+  # Required for pyDirect httpserver USER_C_MODULES (modwebfiles.c uses esp_vfs_littlefs)
+  joltwallet/littlefs: "^1.0.0"
+  # Required for pyDirect husarnet VPN module
+  husarnet/esp_husarnet: "^0.0.15"
+  # Required for pyDirect httpserver USER_C_MODULES (modwebrepl.c uses cbor)
+  espressif/cbor: "^0.6.0"
+  # Required for pyDirect webrtc USER_C_MODULES (modwebrtc.c uses esp_peer for WebRTC)
+  espressif/esp_peer: "^1.2.2"
+  # Required for pyDirect usbmodem module (USB 4G modem with PPP)
+  espressif/iot_usbh_modem:
+    version: "^2.0.0"
+    rules:
+      - if: "target in [esp32s2, esp32s3, esp32p4]"
   idf:
     version: ">=5.2.0"
-- 
2.39.0
